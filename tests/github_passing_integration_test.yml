name: Rust

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Say hello
      run: echo hello
    - name: Say goodbye
      run: echo goodbye
